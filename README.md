# extract_most_recent_row
Extracts most recent row from csv, assuming header includes 'date'

# Example
`python -i file_to_search.csv -o file_to_store_result.csv`

# Warning
Will overwrite output file if it exists. 
